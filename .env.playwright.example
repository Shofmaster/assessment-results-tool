# Playwright auth (for `npm run test:auth:save`).
# Copy to `.env.playwright` (gitignored) and fill in real values.
# Use an account that already exists in your Clerk app.
PLAYWRIGHT_AUTH_EMAIL=test-user@example.com
PLAYWRIGHT_AUTH_PASSWORD=replace-with-a-test-password
